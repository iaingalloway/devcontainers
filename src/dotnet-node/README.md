# Dotnet and Node devcontainer

A devcontainer with both dotnet and node for hybrid development.

## Build

To build the image locally, run the following command:

```bash
devcontainer build --workspace-folder . --image-name ghcr.io/iaingalloway/devcontainers/dotnet-node:local
```
